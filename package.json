{
  "name": "akropolys-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm --filter \"*\" build",
    "dev": "pnpm --filter \"*\" dev"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
